multiproject {
  rootFolder '..'
  gitBase 'https://github.com/akhikhl'
  project name: 'gasmine', dependsOn: 'gino', build: 'libs', buildTasks: [ 'install' ], examples: true, uploadArchives: true
  project name: 'gino', dependsOn: 'gradle-onejar', build: 'libs', buildTasks: [ 'install' ], examples: true, uploadArchives: true
  project name: 'gradle-onejar', build: 'libs', buildTasks: [ 'install' ], examples: true, uploadArchives: true
  project name: 'gretty', build: 'libs', buildTasks: [ 'install' ], examples: true, uploadArchives: true
}
